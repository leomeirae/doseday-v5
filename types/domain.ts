// Tipos de domínio — preenchidos progressivamente nos prompts de features
export type Dose = Record<string, never>
export type CheckIn = Record<string, never>
export type WeightEntry = Record<string, never>
