# Diário V1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir o placeholder `app/(tabs)/diario.tsx` por tela funcional com Quick Log 1-tap, Check-in diário completo e timeline histórico, conectada às tabelas `quick_logs` e `daily_checkins` do Supabase V4.

**Architecture:** Três seções verticais na tela principal (ScrollView) com modais em sheets separados (`/diario/quick-log` e `/diario/checkin`). Dados carregados por `useDiarioSummary` (React Query, staleTime 5min). Dois fluxos de WRITE: quick log 1-tap direto na tela + check-in completo via modal sheet.

**Tech Stack:** React Native + Expo Router, Supabase JS v2, React Query, Zod, Reanimated 4 (chip pulse), expo-haptics, date-fns, react-native-devtools-mcp (17 testes)

---

## Contexto e Restrições

- **Tabelas V4 usadas:** `quick_logs` (quick log 1-tap) + `daily_checkins` (check-in completo)
- **Tabelas PROIBIDAS neste prompt:** `symptom_logs`, `lifestyle_logs`, `lifestyle_triggers`
- **Edge functions PROIBIDAS neste prompt:** `generate-checkin-insight`, `memory-daily-insight` (ficam pro Prompt 16)
- **Sem migrations:** schema V4 é suficiente
- **Sem UPSERT:** 1 check-in por dia — UNIQUE constraint `(user_id, date)` no DB. UI previne via `todayCheckin !== null`
- **Vital Mint Rarity:** `colors.brand` APENAS no CTA "Fazer check-in". Zero na timeline, chips, EmotionalStatePicker
- **Zero BlurView** em área de conteúdo
- **Zero `as any` / `// @ts-ignore`**
- **Credenciais de teste:** `leonardo@teste.com` / `123456`
- **Sem RNGH instalado:** usar `Pressable.onLongPress` (não `GestureDetector`)

## Schema Confirmado via MCP (pré-execução)

### `quick_logs`
| Coluna | Tipo | Notas |
|---|---|---|
| `user_id` | uuid NOT NULL | RLS filter |
| `log_type` | text NOT NULL | enum via Zod |
| `intensity` | integer | **Default=1 no DB** — sempre passar `intensity: 2` no 1-tap |
| `notes` | text nullable | |
| `logged_at` | timestamptz NOT NULL | passar `new Date().toISOString()` |

### `daily_checkins`
| Coluna | Tipo | Notas |
|---|---|---|
| `user_id` | uuid NOT NULL | RLS filter |
| `date` | date NOT NULL | `YYYY-MM-DD` |
| `emotional_state` | text nullable | enum EMOTIONAL_STATES |
| `emotional_intensity` | integer nullable | 1-5, default 3 |
| `days_since_last_dose` | integer nullable | calculado no client |
| `treatment_week` | integer nullable | calculado no client |
| `current_dose_mg` | numeric nullable | `profile.currentDose` |
| `has_adverse_reaction` | boolean | Default=false. Derivar de `symptoms.length > 0` |
| `symptoms` | ARRAY (text[]) | Passar `[]` nunca `null` |
| `symptom_triggers` | ARRAY (text[]) | Idem |
| `notes` | text nullable | max 1000 chars |
| `data_quality_score` | integer | Default=1. Usar `symptoms.length > 0 ? 2 : 1` |
| `(user_id, date)` | UNIQUE | Dois constraints redundantes — seguro para V1 |

## Shape Brief (Voice & Tone — Persona Mariana)

| Elemento | Copy |
|---|---|
| Título da tela | `"Diário"` |
| Seção quick log | `"Registro rápido"` |
| Seção check-in | `"Check-in do dia"` |
| CheckinCard — sem check-in | `"Como você está hoje?"` |
| CheckinCard — subtítulo | `"Anote como foi seu dia"` (ou `"Você está na semana X"` se `treatmentWeek != null`) |
| CTA CheckinCard | `"Fazer check-in"` |
| CheckinCard — com check-in | Emoji do humor + lista de sintomas + triggers (sem CTA, sem edição V1) |
| Seção histórico | `"Histórico"` |
| Empty state histórico | `"Seu diário vai aparecer aqui. Comece registrando como você está hoje."` |
| Toast quick log (sucesso) | `"Registrado"` |
| Toast check-in (sucesso) | `"Check-in registrado"` |
| Erro genérico | `"Não consegui salvar. Sem internet?"` |
| Header modal quick-log | `"Registrar [LABEL]"` ex: `"Registrar Náusea"` |
| Header modal check-in | `"Como foi seu dia?"` |

---

## Arquivos

| Arquivo | Ação |
|---|---|
| `components/doses/SectionHeader.tsx` → `components/ui/SectionHeader.tsx` | Mover (mini-refactor pré-feature) |
| `lib/validation/diarioSchemas.ts` | Criar |
| `lib/supabase/queries/diario.ts` | Criar |
| `hooks/useDiarioSummary.ts` | Criar |
| `hooks/useRegisterQuickLog.ts` | Criar |
| `hooks/useRegisterCheckin.ts` | Criar |
| `components/diario/QuickLogChips.tsx` | Criar |
| `components/diario/EmotionalStatePicker.tsx` | Criar |
| `components/diario/SymptomsMultiSelect.tsx` | Criar |
| `components/diario/TriggersMultiSelect.tsx` | Criar |
| `components/diario/CheckinCard.tsx` | Criar |
| `components/diario/DiarioTimelineItem.tsx` | Criar |
| `app/diario/checkin.tsx` | Criar |
| `app/diario/quick-log.tsx` | Criar |
| `app/(tabs)/diario.tsx` | Substituir (placeholder → funcional) |
| `app/_layout.tsx` | Modificar (+2 Stack.Screen modais) |

---

## Task 1 — Mini-refactor: mover SectionHeader para `components/ui/`

**Files:**
- Move: `components/doses/SectionHeader.tsx` → `components/ui/SectionHeader.tsx`
- Check & update: todos os imports existentes de `SectionHeader`

**Motivo:** `SectionHeader` é genérico (já existe em doses, será usado em diário). Mover antes de criar novos componentes.

- [ ] **Step 1: Ler o arquivo atual**

```bash
cat components/doses/SectionHeader.tsx
```

Verificar se tem qualquer import ou lógica específica de doses (cores, tokens de doses, etc).

- [ ] **Step 2: Verificar imports existentes**

```bash
grep -rn "doses/SectionHeader" components/ app/ hooks/ lib/
```

Anotar todos os arquivos que importam de `components/doses/SectionHeader`.

- [ ] **Step 3: Mover o arquivo**

```bash
cp components/doses/SectionHeader.tsx components/ui/SectionHeader.tsx
```

- [ ] **Step 4: Atualizar imports nos arquivos afetados**

Para cada arquivo encontrado no Step 2, substituir:
```typescript
// DE:
import { SectionHeader } from '@components/doses/SectionHeader'
// PARA:
import { SectionHeader } from '@components/ui/SectionHeader'
```

- [ ] **Step 5: Remover o arquivo original**

```bash
rm components/doses/SectionHeader.tsx
```

- [ ] **Step 6: Verificar type-check**

```bash
npm run type-check
```

Esperado: 0 erros.

- [ ] **Step 7: Commit**

```bash
git add components/ui/SectionHeader.tsx components/doses/
git add -u  # captura o rm
git commit -m "refactor(ui): move SectionHeader de components/doses/ para components/ui/"
```

---

## Task 2 — Schemas e tipos (`lib/validation/diarioSchemas.ts`)

**Files:**
- Create: `lib/validation/diarioSchemas.ts`

- [ ] **Step 1: Criar o arquivo**

```typescript
// lib/validation/diarioSchemas.ts
import { z } from 'zod'

export const QUICK_LOG_TYPES = [
  'nausea', 'headache', 'fatigue', 'diarrhea', 'constipation',
  'heartburn', 'injection_pain', 'alcohol', 'feeling_good', 'other',
] as const

export type QuickLogType = typeof QUICK_LOG_TYPES[number]

export const QUICK_LOG_LABELS: Record<QuickLogType, string> = {
  nausea: 'Náusea',
  headache: 'Dor de cabeça',
  fatigue: 'Cansaço',
  diarrhea: 'Diarreia',
  constipation: 'Constipação',
  heartburn: 'Azia',
  injection_pain: 'Dor na injeção',
  alcohol: 'Bebi álcool',
  feeling_good: 'Bem hoje',
  other: 'Outro',
}

export const INTENSITY_LABELS: Record<1 | 2 | 3, string> = {
  1: 'Leve',
  2: 'Moderado',
  3: 'Forte',
}

// Tipos clínicos para SymptomsMultiSelect (exclui feeling_good e alcohol)
export const SYMPTOM_TYPES = [
  'nausea', 'headache', 'fatigue', 'diarrhea',
  'constipation', 'heartburn', 'injection_pain', 'other',
] as const satisfies readonly QuickLogType[]

export type SymptomType = typeof SYMPTOM_TYPES[number]

export const EMOTIONAL_STATES = ['terrible', 'bad', 'ok', 'good', 'great'] as const
export type EmotionalState = typeof EMOTIONAL_STATES[number]

export const EMOTIONAL_LABELS: Record<EmotionalState, string> = {
  terrible: 'Péssimo',
  bad: 'Mal',
  ok: 'Ok',
  good: 'Bem',
  great: 'Ótimo',
}

export const EMOTIONAL_EMOJIS: Record<EmotionalState, string> = {
  terrible: '😣',
  bad: '😕',
  ok: '😐',
  good: '🙂',
  great: '😊',
}

export const TRIGGERS = [
  'alcohol', 'fatty_food', 'sweet_excess', 'fasting',
  'poor_water', 'fast_eating', 'stress',
] as const

export type Trigger = typeof TRIGGERS[number]

export const TRIGGER_LABELS: Record<Trigger, string> = {
  alcohol: 'Álcool',
  fatty_food: 'Comida gordurosa',
  sweet_excess: 'Excesso de doces',
  fasting: 'Jejum',
  poor_water: 'Pouca água',
  fast_eating: 'Comer rápido',
  stress: 'Estresse',
}

export const quickLogSchema = z.object({
  logType: z.enum(QUICK_LOG_TYPES),
  intensity: z.union([z.literal(1), z.literal(2), z.literal(3)]).default(2),
  notes: z.string().max(500).optional(),
  loggedAt: z
    .date()
    .refine((d) => d.getTime() <= Date.now(), {
      message: 'Não é possível registrar no futuro',
    })
    .default(() => new Date()),
})

export const checkinSchema = z.object({
  date: z
    .date()
    .refine(
      (d) => d.toISOString().slice(0, 10) <= new Date().toISOString().slice(0, 10),
      { message: 'Não é possível fazer check-in pra data futura' }
    )
    .default(() => new Date()),
  emotionalState: z.enum(EMOTIONAL_STATES),
  emotionalIntensity: z.number().int().min(1).max(5).default(3),
  symptoms: z.array(z.enum(SYMPTOM_TYPES)).default([]),
  symptomTriggers: z.array(z.enum(TRIGGERS)).default([]),
  notes: z.string().max(1000).optional(),
})

export type QuickLogInput = z.infer<typeof quickLogSchema>
export type CheckinInput = z.infer<typeof checkinSchema>
```

- [ ] **Step 2: Verificar type-check**

```bash
npm run type-check
```

Esperado: 0 erros.

---

## Task 3 — Queries (`lib/supabase/queries/diario.ts`)

**Files:**
- Create: `lib/supabase/queries/diario.ts`

**Pontos críticos:**
- `intensity: 2` explícito no quick log 1-tap (DB default é 1)
- `symptoms: input.symptoms` → sempre array, nunca null
- `has_adverse_reaction: input.symptoms.length > 0` derivado no client
- `data_quality_score: input.symptoms.length > 0 ? 2 : 1`
- `maybeSingle()` para today's check-in (não lança erro quando null)
- Contexto clínico calculado antes do INSERT — ver função `buildTreatmentContext`

- [ ] **Step 1: Criar o arquivo**

```typescript
// lib/supabase/queries/diario.ts
import { supabase } from '@lib/supabase/client'
import type { QuickLogInput, CheckinInput, QuickLogType, EmotionalState, Trigger } from '@lib/validation/diarioSchemas'

export type QuickLogRecord = {
  id: string
  logType: QuickLogType
  intensity: 1 | 2 | 3
  notes: string | null
  loggedAt: Date
}

export type CheckinRecord = {
  id: string
  date: string // YYYY-MM-DD
  emotionalState: EmotionalState | null
  emotionalIntensity: number | null
  symptoms: SymptomType[]
  symptomTriggers: Trigger[]
  hasAdverseReaction: boolean
  notes: string | null
  createdAt: Date
}

export type DiarioSummary = {
  todayCheckin: CheckinRecord | null
  recentQuickLogs: QuickLogRecord[]   // últimos 7 dias
  recentCheckins: CheckinRecord[]     // últimos 30 dias, sem o de hoje
}

export type TreatmentContext = {
  daysSinceLastDose: number | null
  treatmentWeek: number | null
  currentDoseMg: number | null
}

export async function getDiarioSummary(userId: string): Promise<DiarioSummary> {
  const todayISO = new Date().toISOString().slice(0, 10)
  const sevenDaysAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString()
  const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000)
    .toISOString()
    .slice(0, 10)

  const [todayRes, qlRes, ckRes] = await Promise.all([
    supabase
      .from('daily_checkins')
      .select(
        'id, date, emotional_state, emotional_intensity, symptoms, symptom_triggers, has_adverse_reaction, notes, created_at'
      )
      .eq('user_id', userId)
      .eq('date', todayISO)
      .maybeSingle(),

    supabase
      .from('quick_logs')
      .select('id, log_type, intensity, notes, logged_at')
      .eq('user_id', userId)
      .gte('logged_at', sevenDaysAgo)
      .order('logged_at', { ascending: false })
      .limit(50),

    supabase
      .from('daily_checkins')
      .select(
        'id, date, emotional_state, emotional_intensity, symptoms, symptom_triggers, has_adverse_reaction, notes, created_at'
      )
      .eq('user_id', userId)
      .neq('date', todayISO)
      .gte('date', thirtyDaysAgo)
      .order('date', { ascending: false })
      .limit(30),
  ])

  if (todayRes.error) throw todayRes.error
  if (qlRes.error) throw qlRes.error
  if (ckRes.error) throw ckRes.error

  return {
    todayCheckin: todayRes.data ? mapCheckinRow(todayRes.data) : null,
    recentQuickLogs: (qlRes.data ?? []).map(mapQuickLogRow),
    recentCheckins: (ckRes.data ?? []).map(mapCheckinRow),
  }
}

export async function registerQuickLog(
  userId: string,
  input: QuickLogInput
): Promise<void> {
  const { error } = await supabase.from('quick_logs').insert({
    user_id: userId,
    log_type: input.logType,
    intensity: input.intensity,    // sempre explícito — DB default é 1, tap usa 2
    notes: input.notes ?? null,
    logged_at: input.loggedAt.toISOString(),
  })
  if (error) throw error
}

export async function registerCheckin(
  userId: string,
  input: CheckinInput,
  ctx: TreatmentContext
): Promise<void> {
  const hasAdverse = input.symptoms.length > 0
  const { error } = await supabase.from('daily_checkins').insert({
    user_id: userId,
    date: input.date.toISOString().slice(0, 10),
    emotional_state: input.emotionalState,
    emotional_intensity: input.emotionalIntensity,
    symptoms: input.symptoms,           // text[] — nunca null
    symptom_triggers: input.symptomTriggers,
    has_adverse_reaction: hasAdverse,
    notes: input.notes ?? null,
    days_since_last_dose: ctx.daysSinceLastDose,
    treatment_week: ctx.treatmentWeek,
    current_dose_mg: ctx.currentDoseMg,
    data_quality_score: hasAdverse ? 2 : 1,
  })
  if (error) throw error
}

// ─── Helpers de cálculo do contexto clínico (chamados no client) ──────────────

export function buildTreatmentContext(
  profile: { currentDose: number | null; treatmentStartDate: string | null } | null,
  lastDoseDate: Date | null
): TreatmentContext {
  const currentDoseMg = profile?.currentDose ?? null

  const treatmentWeek =
    profile?.treatmentStartDate
      ? Math.floor(
          (Date.now() - new Date(profile.treatmentStartDate).getTime()) /
            (7 * 24 * 60 * 60 * 1000)
        ) + 1
      : null

  const daysSinceLastDose = lastDoseDate
    ? Math.floor((Date.now() - lastDoseDate.getTime()) / (24 * 60 * 60 * 1000))
    : null

  return { currentDoseMg, treatmentWeek, daysSinceLastDose }
}

// ─── Mappers ──────────────────────────────────────────────────────────────────

function mapCheckinRow(row: {
  id: string
  date: string
  emotional_state: string | null
  emotional_intensity: number | null
  symptoms: string[] | null
  symptom_triggers: string[] | null
  has_adverse_reaction: boolean | null
  notes: string | null
  created_at: string | null
}): CheckinRecord {
  return {
    id: row.id,
    date: row.date,
    emotionalState: (row.emotional_state as EmotionalState) ?? null,
    emotionalIntensity: row.emotional_intensity,
    symptoms: (row.symptoms ?? []) as SymptomType[],
    symptomTriggers: (row.symptom_triggers ?? []) as Trigger[],
    hasAdverseReaction: row.has_adverse_reaction ?? false,
    notes: row.notes,
    createdAt: new Date(row.created_at ?? Date.now()),
  }
}

function mapQuickLogRow(row: {
  id: string
  log_type: string
  intensity: number | null
  notes: string | null
  logged_at: string
}): QuickLogRecord {
  return {
    id: row.id,
    logType: row.log_type as QuickLogType,
    intensity: (row.intensity ?? 2) as 1 | 2 | 3,
    notes: row.notes,
    loggedAt: new Date(row.logged_at),
  }
}
```

> **Nota:** `SymptomType` e `Trigger` precisam ser importados de `@lib/validation/diarioSchemas` — adicionar ao import no topo do arquivo.

- [ ] **Step 2: Verificar type-check**

```bash
npm run type-check
```

Esperado: 0 erros.

---

## Task 4 — Hooks

**Files:**
- Create: `hooks/useDiarioSummary.ts`
- Create: `hooks/useRegisterQuickLog.ts`
- Create: `hooks/useRegisterCheckin.ts`

- [ ] **Step 1: Criar `hooks/useDiarioSummary.ts`**

```typescript
import { useQuery } from '@tanstack/react-query'
import { useSession } from './useSession'
import { getDiarioSummary } from '@lib/supabase/queries/diario'

export function useDiarioSummary() {
  const { session } = useSession()
  const userId = session?.user?.id

  return useQuery({
    queryKey: ['diarioSummary', userId],
    queryFn: () => {
      if (!userId) throw new Error('No user')
      return getDiarioSummary(userId)
    },
    enabled: !!userId,
  })
}
```

- [ ] **Step 2: Criar `hooks/useRegisterQuickLog.ts`**

```typescript
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { useSession } from './useSession'
import { registerQuickLog } from '@lib/supabase/queries/diario'
import type { QuickLogInput } from '@lib/validation/diarioSchemas'

export function useRegisterQuickLog() {
  const { session } = useSession()
  const queryClient = useQueryClient()
  const userId = session?.user?.id

  return useMutation({
    mutationFn: (input: QuickLogInput) => {
      if (!userId) throw new Error('No user')
      return registerQuickLog(userId, input)
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['diarioSummary', userId] })
    },
  })
}
```

- [ ] **Step 3: Criar `hooks/useRegisterCheckin.ts`**

```typescript
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { useSession } from './useSession'
import { registerCheckin } from '@lib/supabase/queries/diario'
import type { CheckinInput, TreatmentContext } from '@lib/supabase/queries/diario'
import type { CheckinInput } from '@lib/validation/diarioSchemas'

export function useRegisterCheckin() {
  const { session } = useSession()
  const queryClient = useQueryClient()
  const userId = session?.user?.id

  return useMutation({
    mutationFn: ({ input, ctx }: { input: CheckinInput; ctx: TreatmentContext }) => {
      if (!userId) throw new Error('No user')
      return registerCheckin(userId, input, ctx)
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['diarioSummary', userId] })
    },
  })
}
```

- [ ] **Step 4: Verificar type-check**

```bash
npm run type-check
```

Esperado: 0 erros.

- [ ] **Step 5: Commit**

```bash
git add lib/validation/diarioSchemas.ts lib/supabase/queries/diario.ts hooks/useDiarioSummary.ts hooks/useRegisterQuickLog.ts hooks/useRegisterCheckin.ts
git commit -m "feat(diario): schemas Zod, queries Supabase, hooks React Query"
```

---

## Task 5 — Componentes base (sem lógica de rede)

**Files:**
- Create: `components/diario/EmotionalStatePicker.tsx`
- Create: `components/diario/SymptomsMultiSelect.tsx`
- Create: `components/diario/TriggersMultiSelect.tsx`
- Create: `components/diario/DiarioTimelineItem.tsx`

### `EmotionalStatePicker.tsx`

```typescript
// components/diario/EmotionalStatePicker.tsx
import { View, Text, Pressable, StyleSheet } from 'react-native'
import { EMOTIONAL_STATES, EMOTIONAL_EMOJIS, EMOTIONAL_LABELS } from '@lib/validation/diarioSchemas'
import type { EmotionalState } from '@lib/validation/diarioSchemas'
import { typography, spacing } from '@lib/theme/tokens'

type Props = {
  value: EmotionalState | null
  onChange: (state: EmotionalState) => void
}

export function EmotionalStatePicker({ value, onChange }: Props) {
  return (
    <View style={styles.row} accessibilityRole="radiogroup" accessibilityLabel="Como você está">
      {EMOTIONAL_STATES.map((state) => {
        const selected = value === state
        return (
          <Pressable
            key={state}
            onPress={() => onChange(state)}
            accessibilityRole="radio"
            accessibilityState={{ checked: selected }}
            accessibilityLabel={EMOTIONAL_LABELS[state]}
            style={({ pressed }) => [styles.item, (selected || pressed) && styles.itemSelected]}
          >
            <Text style={styles.emoji}>{EMOTIONAL_EMOJIS[state]}</Text>
            <Text style={[styles.label, selected && styles.labelSelected]}>
              {EMOTIONAL_LABELS[state]}
            </Text>
          </Pressable>
        )
      })}
    </View>
  )
}

const styles = StyleSheet.create({
  row: { flexDirection: 'row', justifyContent: 'space-between' },
  item: { alignItems: 'center', gap: spacing.xxs, opacity: 0.5, flex: 1 },
  itemSelected: { opacity: 1 },
  emoji: { fontSize: 28 },
  label: { ...typography.caption, color: colors.textSecondary },
  labelSelected: { color: colors.textPrimary },
})
```

> Adicionar import de `colors` e `colors` do `@lib/theme/tokens`.

### `SymptomsMultiSelect.tsx`

```typescript
// components/diario/SymptomsMultiSelect.tsx
import { ScrollView, Pressable, Text, StyleSheet, View } from 'react-native'
import { SYMPTOM_TYPES, QUICK_LOG_LABELS } from '@lib/validation/diarioSchemas'
import type { SymptomType } from '@lib/validation/diarioSchemas'
import { colors, typography, spacing, radius } from '@lib/theme/tokens'

type Props = {
  selected: SymptomType[]
  onChange: (symptoms: SymptomType[]) => void
}

export function SymptomsMultiSelect({ selected, onChange }: Props) {
  function toggle(type: SymptomType) {
    if (selected.includes(type)) {
      onChange(selected.filter((s) => s !== type))
    } else {
      onChange([...selected, type])
    }
  }

  return (
    <ScrollView
      horizontal
      showsHorizontalScrollIndicator={false}
      contentContainerStyle={styles.row}
    >
      {SYMPTOM_TYPES.map((type) => {
        const isSelected = selected.includes(type)
        return (
          <Pressable
            key={type}
            onPress={() => toggle(type)}
            accessibilityRole="checkbox"
            accessibilityState={{ checked: isSelected }}
            accessibilityLabel={QUICK_LOG_LABELS[type]}
            style={({ pressed }) => [
              styles.chip,
              isSelected && styles.chipSelected,
              pressed && styles.chipPressed,
            ]}
          >
            <Text style={[styles.label, isSelected && styles.labelSelected]}>
              {QUICK_LOG_LABELS[type]}
            </Text>
          </Pressable>
        )
      })}
    </ScrollView>
  )
}

const styles = StyleSheet.create({
  row: { flexDirection: 'row', gap: spacing.xs, paddingVertical: spacing.xxs },
  chip: {
    paddingHorizontal: spacing.md,
    paddingVertical: spacing.xs,
    borderRadius: radius.full,
    backgroundColor: colors.bgElevated,
    borderWidth: 0.5,
    borderColor: 'rgba(255,255,255,0.12)',
    minHeight: 44,
    justifyContent: 'center',
  },
  chipSelected: {
    borderColor: colors.textPrimary,
    borderWidth: 1,
  },
  chipPressed: { transform: [{ scale: 0.96 }] },
  label: { ...typography.label, color: colors.textSecondary },
  labelSelected: { color: colors.textPrimary },
})
```

### `TriggersMultiSelect.tsx`

Idêntico ao `SymptomsMultiSelect` mas usa `TRIGGERS` e `TRIGGER_LABELS`. Criar seguindo o mesmo padrão com `Trigger` como tipo genérico.

### `DiarioTimelineItem.tsx`

```typescript
// components/diario/DiarioTimelineItem.tsx
import { View, Text, StyleSheet } from 'react-native'
import { formatDistanceToNow, format } from 'date-fns'
import { ptBR } from 'date-fns/locale'
import {
  QUICK_LOG_LABELS, INTENSITY_LABELS, EMOTIONAL_EMOJIS, EMOTIONAL_LABELS,
  QUICK_LOG_LABELS as QL, TRIGGER_LABELS,
} from '@lib/validation/diarioSchemas'
import type { QuickLogRecord, CheckinRecord } from '@lib/supabase/queries/diario'
import { colors, typography, spacing, radius } from '@lib/theme/tokens'

type Props =
  | { kind: 'quick_log'; item: QuickLogRecord }
  | { kind: 'checkin'; item: CheckinRecord }

export function DiarioTimelineItem(props: Props) {
  if (props.kind === 'quick_log') {
    const { item } = props
    const timeAgo = formatDistanceToNow(item.loggedAt, { addSuffix: true, locale: ptBR })
    return (
      <View style={styles.container}>
        <View style={styles.dot} accessibilityElementsHidden />
        <View style={styles.content}>
          <Text style={styles.title}>
            {QUICK_LOG_LABELS[item.logType]} · {INTENSITY_LABELS[item.intensity]}
          </Text>
          <Text style={styles.meta}>{timeAgo}</Text>
          {!!item.notes && <Text style={styles.notes}>{item.notes}</Text>}
        </View>
      </View>
    )
  }

  const { item } = props
  const dateLabel = format(new Date(item.date), "d 'de' MMM", { locale: ptBR })
  const emoji = item.emotionalState ? EMOTIONAL_EMOJIS[item.emotionalState] : '—'
  const symptomsLabel = item.symptoms.map((s) => QUICK_LOG_LABELS[s]).join(', ')
  const triggersLabel = item.symptomTriggers.map((t) => TRIGGER_LABELS[t]).join(', ')

  return (
    <View style={styles.container}>
      <Text style={styles.emojiIcon} accessibilityElementsHidden>{emoji}</Text>
      <View style={styles.content}>
        <Text style={styles.title}>
          Check-in · {dateLabel}
        </Text>
        {symptomsLabel.length > 0 && (
          <Text style={styles.meta}>{symptomsLabel}</Text>
        )}
        {triggersLabel.length > 0 && (
          <Text style={styles.meta}>Gatilhos: {triggersLabel}</Text>
        )}
        {!!item.notes && <Text style={styles.notes}>{item.notes}</Text>}
      </View>
    </View>
  )
}

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: spacing.sm,
    alignItems: 'flex-start',
    paddingVertical: spacing.sm,
    paddingHorizontal: spacing.md,
    backgroundColor: colors.bgElevated,
    borderRadius: radius.md,
    borderWidth: 0.5,
    borderColor: 'rgba(255,255,255,0.06)',
  },
  dot: {
    width: 8, height: 8, borderRadius: 4,
    backgroundColor: colors.textTertiary,
    marginTop: 6,
  },
  emojiIcon: { fontSize: 20, lineHeight: 24 },
  content: { flex: 1, gap: spacing.xxs },
  title: { ...typography.label, color: colors.textPrimary },
  meta: { ...typography.caption, color: colors.textSecondary },
  notes: { ...typography.caption, color: colors.textTertiary, fontStyle: 'italic' },
})
```

- [ ] **Step 1:** Criar `components/diario/EmotionalStatePicker.tsx`
- [ ] **Step 2:** Criar `components/diario/SymptomsMultiSelect.tsx`
- [ ] **Step 3:** Criar `components/diario/TriggersMultiSelect.tsx` (seguir padrão de SymptomsMultiSelect com TRIGGERS/TRIGGER_LABELS)
- [ ] **Step 4:** Criar `components/diario/DiarioTimelineItem.tsx`
- [ ] **Step 5:** `npm run type-check` → 0 erros

---

## Task 6 — `QuickLogChips.tsx` com Reanimated 4

**Files:**
- Create: `components/diario/QuickLogChips.tsx`

**Padrão de animação (Reanimated 4 — sem `runOnJS`):**

```typescript
// components/diario/QuickLogChips.tsx
import { ScrollView, Pressable, Text, StyleSheet } from 'react-native'
import Animated, { useSharedValue, withSpring, useAnimatedStyle } from 'react-native-reanimated'
import * as Haptics from 'expo-haptics'
import { useRouter } from 'expo-router'
import { QUICK_LOG_TYPES, QUICK_LOG_LABELS } from '@lib/validation/diarioSchemas'
import type { QuickLogType } from '@lib/validation/diarioSchemas'
import { colors, typography, spacing, radius } from '@lib/theme/tokens'

type Props = {
  onQuickLog: (logType: QuickLogType) => void
  disabled?: boolean
}

export function QuickLogChips({ onQuickLog, disabled }: Props) {
  const router = useRouter()
  return (
    <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.row}>
      {QUICK_LOG_TYPES.map((logType) => (
        <AnimatedChip
          key={logType}
          label={QUICK_LOG_LABELS[logType]}
          disabled={disabled}
          onTap={() => onQuickLog(logType)}
          onLongPress={() =>
            router.push({ pathname: '/diario/quick-log', params: { type: logType } })
          }
        />
      ))}
    </ScrollView>
  )
}

function AnimatedChip({
  label, onTap, onLongPress, disabled,
}: {
  label: string
  onTap: () => void
  onLongPress: () => void
  disabled?: boolean
}) {
  const scale = useSharedValue(1)
  const animStyle = useAnimatedStyle(() => ({ transform: [{ scale: scale.value }] }))

  function handlePress() {
    if (disabled) return
    Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)
    onTap()
    // Animação pura no UI thread — sem runOnJS (removido no Reanimated 4)
    scale.value = withSpring(0.95, { damping: 15 }, () => {
      scale.value = withSpring(1)
    })
  }

  return (
    <Animated.View style={animStyle}>
      <Pressable
        onPress={handlePress}
        onLongPress={onLongPress}
        delayLongPress={400}
        disabled={disabled}
        accessibilityRole="button"
        accessibilityLabel={label}
        accessibilityHint="Toque para registrar. Toque longo para detalhar."
        style={styles.chip}
      >
        <Text style={styles.label}>{label}</Text>
      </Pressable>
    </Animated.View>
  )
}

const styles = StyleSheet.create({
  row: { flexDirection: 'row', gap: spacing.xs, paddingVertical: spacing.xxs },
  chip: {
    paddingHorizontal: spacing.md,
    paddingVertical: spacing.sm,
    borderRadius: radius.full,
    backgroundColor: colors.bgElevated,
    borderWidth: 0.5,
    borderColor: 'rgba(255,255,255,0.12)',
    minHeight: 44,
    justifyContent: 'center',
  },
  label: { ...typography.label, color: colors.textPrimary },
})
```

- [ ] **Step 1:** Criar `components/diario/QuickLogChips.tsx`
- [ ] **Step 2:** `npm run type-check` → 0 erros

---

## Task 7 — `CheckinCard.tsx`

**Files:**
- Create: `components/diario/CheckinCard.tsx`

**Dois estados baseados em `todayCheckin: CheckinRecord | null`:**

```typescript
// components/diario/CheckinCard.tsx
import { View, Text, Pressable, StyleSheet } from 'react-native'
import type { CheckinRecord } from '@lib/supabase/queries/diario'
import {
  EMOTIONAL_EMOJIS, EMOTIONAL_LABELS,
  QUICK_LOG_LABELS, TRIGGER_LABELS,
} from '@lib/validation/diarioSchemas'
import { colors, typography, spacing, radius } from '@lib/theme/tokens'

type Props = {
  todayCheckin: CheckinRecord | null
  treatmentWeek: number | null
  onPressCTA: () => void
}

export function CheckinCard({ todayCheckin, treatmentWeek, onPressCTA }: Props) {
  if (todayCheckin) {
    // Estado: check-in já feito — mostrar resumo
    const emoji = todayCheckin.emotionalState
      ? EMOTIONAL_EMOJIS[todayCheckin.emotionalState]
      : '—'
    const label = todayCheckin.emotionalState
      ? EMOTIONAL_LABELS[todayCheckin.emotionalState]
      : ''
    const symptomsText = todayCheckin.symptoms
      .map((s) => QUICK_LOG_LABELS[s])
      .join(', ')
    const triggersText = todayCheckin.symptomTriggers
      .map((t) => TRIGGER_LABELS[t])
      .join(', ')

    return (
      <View style={styles.card} accessibilityLabel={`Check-in de hoje: ${label}`}>
        <Text style={styles.emoji}>{emoji}</Text>
        <View style={styles.resumeContent}>
          <Text style={styles.resumeTitle}>{label}</Text>
          {symptomsText.length > 0 && (
            <Text style={styles.resumeDetail}>{symptomsText}</Text>
          )}
          {triggersText.length > 0 && (
            <Text style={styles.resumeDetail}>Gatilhos: {triggersText}</Text>
          )}
        </View>
      </View>
    )
  }

  // Estado: sem check-in — mostrar CTA
  const subtitle = treatmentWeek != null
    ? `Você está na semana ${treatmentWeek}`
    : 'Anote como foi seu dia'

  return (
    <View style={styles.card}>
      <Text style={styles.ctaTitle}>Como você está hoje?</Text>
      <Text style={styles.ctaSubtitle}>{subtitle}</Text>
      <Pressable
        onPress={onPressCTA}
        accessibilityRole="button"
        accessibilityLabel="Fazer check-in"
        style={({ pressed }) => [styles.ctaButton, pressed && styles.ctaButtonPressed]}
      >
        <Text style={styles.ctaButtonLabel}>Fazer check-in</Text>
      </Pressable>
    </View>
  )
}

const styles = StyleSheet.create({
  card: {
    backgroundColor: colors.bgElevated,
    borderRadius: radius.lg,
    padding: spacing.lg,
    gap: spacing.sm,
    borderWidth: 0.5,
    borderColor: 'rgba(255,255,255,0.06)',
  },
  // Estado: check-in feito
  emoji: { fontSize: 32 },
  resumeContent: { gap: spacing.xxs },
  resumeTitle: { ...typography.subheadline, color: colors.textPrimary },
  resumeDetail: { ...typography.caption, color: colors.textSecondary },
  // Estado: CTA
  ctaTitle: { ...typography.subheadline, color: colors.textPrimary },
  ctaSubtitle: { ...typography.body, color: colors.textSecondary },
  ctaButton: {
    backgroundColor: colors.brand,   // ÚNICO uso de brand na tela — regra Vital Mint Rarity
    borderRadius: radius.md,
    paddingVertical: spacing.sm,
    alignItems: 'center',
    marginTop: spacing.xs,
  },
  ctaButtonPressed: { opacity: 0.85 },
  ctaButtonLabel: { ...typography.label, color: '#000000' },
})
```

- [ ] **Step 1:** Criar `components/diario/CheckinCard.tsx`
- [ ] **Step 2:** `npm run type-check` → 0 erros
- [ ] **Step 3: Commit**

```bash
git add components/diario/
git commit -m "feat(diario): componentes QuickLogChips, CheckinCard, EmotionalStatePicker, SymptomsMultiSelect, TriggersMultiSelect, DiarioTimelineItem"
```

---

## Task 8 — Modais

**Files:**
- Create: `app/diario/checkin.tsx`
- Create: `app/diario/quick-log.tsx`
- Modify: `app/_layout.tsx`

### `app/_layout.tsx` — adicionar Stack.Screens

Após o `<Stack.Screen name="dose/registrar" ... />` existente, adicionar:

```typescript
<Stack.Screen
  name="diario/checkin"
  options={{ presentation: 'modal', headerShown: false }}
/>
<Stack.Screen
  name="diario/quick-log"
  options={{ presentation: 'modal', headerShown: false }}
/>
```

### `app/diario/checkin.tsx`

Segue o padrão de `app/dose/registrar.tsx` (SafeAreaView + Header ✕ + KeyboardAvoidingView + ScrollView + Footer AuthButton):

```typescript
// app/diario/checkin.tsx
import { useState } from 'react'
import {
  View, Text, ScrollView, KeyboardAvoidingView,
  Pressable, Platform, StyleSheet,
} from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
import { useRouter } from 'expo-router'
import { SymbolView } from 'expo-symbols'
import { colors, typography, spacing, radius } from '@lib/theme/tokens'
import { TextField } from '@components/ui/TextField'
import { AuthButton } from '@components/ui/AuthButton'
import { EmotionalStatePicker } from '@components/diario/EmotionalStatePicker'
import { SymptomsMultiSelect } from '@components/diario/SymptomsMultiSelect'
import { TriggersMultiSelect } from '@components/diario/TriggersMultiSelect'
import { useRegisterCheckin } from '@hooks/useRegisterCheckin'
import { useProfile } from '@hooks/useProfile'
import { useDoseSummary } from '@hooks/useDoseSummary'
import { showSuccessToast, showErrorToast } from '@lib/utils/showToast'
import { mapQueryError } from '@lib/supabase/queries/errors'
import { buildTreatmentContext } from '@lib/supabase/queries/diario'
import { checkinSchema } from '@lib/validation/diarioSchemas'
import type { EmotionalState, SymptomType, Trigger } from '@lib/validation/diarioSchemas'

export default function CheckinScreen() {
  const router = useRouter()
  const { data: profile } = useProfile()
  const { data: doseSummary } = useDoseSummary()
  const { mutate, isPending } = useRegisterCheckin()

  const [emotionalState, setEmotionalState] = useState<EmotionalState | null>(null)
  const [symptoms, setSymptoms] = useState<SymptomType[]>([])
  const [triggers, setTriggers] = useState<Trigger[]>([])
  const [notes, setNotes] = useState('')
  const [errors, setErrors] = useState<Record<string, string>>({})

  function handleSubmit() {
    if (!emotionalState) {
      setErrors({ emotionalState: 'Selecione como você está se sentindo.' })
      return
    }
    const parsed = checkinSchema.safeParse({
      emotionalState,
      symptoms,
      symptomTriggers: triggers,
      notes: notes.trim() === '' ? undefined : notes.trim(),
    })
    if (!parsed.success) {
      const fieldErrors: Record<string, string> = {}
      for (const issue of parsed.error.issues) {
        const key = String(issue.path[0] ?? 'form')
        if (!fieldErrors[key]) fieldErrors[key] = issue.message
      }
      setErrors(fieldErrors)
      return
    }
    setErrors({})
    const lastDoseDate = doseSummary?.history[0]?.applicationDate ?? null
    const ctx = buildTreatmentContext(profile ?? null, lastDoseDate)
    mutate(
      { input: parsed.data, ctx },
      {
        onSuccess: () => {
          showSuccessToast('Check-in registrado')
          router.back()
        },
        onError: (err) => {
          showErrorToast(mapQueryError(err))
        },
      }
    )
  }

  return (
    <SafeAreaView style={styles.safe} edges={['top', 'bottom']}>
      <View style={styles.header}>
        <Pressable onPress={() => router.back()} hitSlop={13} accessibilityLabel="Fechar" accessibilityRole="button">
          <SymbolView name="xmark" size={18} tintColor={colors.textSecondary} />
        </Pressable>
        <Text style={styles.headerTitle}>Como foi seu dia?</Text>
        <View style={styles.headerSpacer} />
      </View>

      <KeyboardAvoidingView style={styles.flex} behavior={Platform.OS === 'ios' ? 'padding' : 'height'}>
        <ScrollView style={styles.flex} contentContainerStyle={styles.scrollContent} keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false}>

          <View style={styles.field}>
            <Text style={styles.fieldLabel}>Como você está?</Text>
            <EmotionalStatePicker value={emotionalState} onChange={setEmotionalState} />
            {!!errors.emotionalState && <Text style={styles.errorText}>{errors.emotionalState}</Text>}
          </View>

          <View style={styles.field}>
            <Text style={styles.fieldLabel}>Sintomas (opcional)</Text>
            <SymptomsMultiSelect selected={symptoms} onChange={setSymptoms} />
          </View>

          <View style={styles.field}>
            <Text style={styles.fieldLabel}>Gatilhos (opcional)</Text>
            <TriggersMultiSelect selected={triggers} onChange={setTriggers} />
          </View>

          <TextField
            label="Observações"
            value={notes}
            onChangeText={setNotes}
            multiline
            numberOfLines={3}
            maxLength={1000}
            placeholder="Opcional"
            error={errors.notes}
            testID="checkin-notes-input"
          />
        </ScrollView>

        <View style={styles.footer}>
          <AuthButton
            label="Salvar check-in"
            onPress={handleSubmit}
            loading={isPending}
            disabled={isPending}
          />
        </View>
      </KeyboardAvoidingView>
    </SafeAreaView>
  )
}

const styles = StyleSheet.create({
  safe: { flex: 1, backgroundColor: colors.bgBase },
  flex: { flex: 1 },
  header: {
    flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between',
    paddingHorizontal: spacing.lg, paddingVertical: spacing.md,
    borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: 'rgba(255,255,255,0.08)',
  },
  headerTitle: { ...typography.title, color: colors.textPrimary },
  headerSpacer: { width: 18 },
  scrollContent: { paddingHorizontal: spacing.lg, paddingTop: spacing.lg, paddingBottom: spacing.xl, gap: spacing.lg },
  field: { gap: spacing.xs },
  fieldLabel: { ...typography.label, color: colors.textSecondary },
  errorText: { ...typography.caption, color: colors.semanticCritical, marginTop: spacing.xxs },
  footer: {
    paddingHorizontal: spacing.lg, paddingTop: spacing.md, paddingBottom: spacing.sm,
    borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: 'rgba(255,255,255,0.08)',
  },
})
```

### `app/diario/quick-log.tsx`

Modal leve acionado por long-press:

```typescript
// app/diario/quick-log.tsx
import { useState } from 'react'
import { View, Text, Pressable, ScrollView, KeyboardAvoidingView, Platform, StyleSheet } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
import { useRouter, useLocalSearchParams } from 'expo-router'
import { SymbolView } from 'expo-symbols'
import { colors, typography, spacing, radius } from '@lib/theme/tokens'
import { TextField } from '@components/ui/TextField'
import { AuthButton } from '@components/ui/AuthButton'
import { useRegisterQuickLog } from '@hooks/useRegisterQuickLog'
import { showSuccessToast, showErrorToast } from '@lib/utils/showToast'
import { mapQueryError } from '@lib/supabase/queries/errors'
import {
  QUICK_LOG_TYPES, QUICK_LOG_LABELS, INTENSITY_LABELS, quickLogSchema,
} from '@lib/validation/diarioSchemas'
import type { QuickLogType } from '@lib/validation/diarioSchemas'
import * as Haptics from 'expo-haptics'

type Intensity = 1 | 2 | 3

export default function QuickLogScreen() {
  const router = useRouter()
  const params = useLocalSearchParams<{ type?: string }>()
  const logType = (QUICK_LOG_TYPES.includes(params.type as QuickLogType)
    ? params.type
    : 'other') as QuickLogType
  const { mutate, isPending } = useRegisterQuickLog()

  const [intensity, setIntensity] = useState<Intensity>(2)
  const [notes, setNotes] = useState('')

  function selectIntensity(i: Intensity) {
    Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)
    setIntensity(i)
  }

  function handleSubmit() {
    const parsed = quickLogSchema.safeParse({ logType, intensity, notes: notes.trim() || undefined })
    if (!parsed.success) return
    mutate(parsed.data, {
      onSuccess: () => { showSuccessToast('Registrado'); router.back() },
      onError: (err) => { showErrorToast(mapQueryError(err)) },
    })
  }

  return (
    <SafeAreaView style={styles.safe} edges={['top', 'bottom']}>
      <View style={styles.header}>
        <Pressable onPress={() => router.back()} hitSlop={13} accessibilityLabel="Fechar" accessibilityRole="button">
          <SymbolView name="xmark" size={18} tintColor={colors.textSecondary} />
        </Pressable>
        <Text style={styles.headerTitle}>Registrar {QUICK_LOG_LABELS[logType]}</Text>
        <View style={styles.headerSpacer} />
      </View>

      <KeyboardAvoidingView style={styles.flex} behavior={Platform.OS === 'ios' ? 'padding' : 'height'}>
        <ScrollView style={styles.flex} contentContainerStyle={styles.scrollContent} keyboardShouldPersistTaps="handled">
          <View style={styles.field}>
            <Text style={styles.fieldLabel}>Tipo</Text>
            <View style={styles.readOnly}>
              <Text style={styles.readOnlyText}>{QUICK_LOG_LABELS[logType]}</Text>
            </View>
          </View>

          <View style={styles.field}>
            <Text style={styles.fieldLabel}>Intensidade</Text>
            <View style={styles.chips}>
              {([1, 2, 3] as Intensity[]).map((i) => (
                <Pressable
                  key={i}
                  onPress={() => selectIntensity(i)}
                  accessibilityRole="button"
                  accessibilityState={{ selected: intensity === i }}
                  accessibilityLabel={INTENSITY_LABELS[i]}
                  style={({ pressed }) => [
                    styles.chip,
                    intensity === i && styles.chipSelected,
                    pressed && styles.chipPressed,
                  ]}
                >
                  <Text style={[styles.chipLabel, intensity === i && styles.chipLabelSelected]}>
                    {INTENSITY_LABELS[i]}
                  </Text>
                </Pressable>
              ))}
            </View>
          </View>

          <TextField
            label="Observações"
            value={notes}
            onChangeText={setNotes}
            multiline
            numberOfLines={3}
            maxLength={500}
            placeholder="Opcional"
            testID="quick-log-notes-input"
          />
        </ScrollView>

        <View style={styles.footer}>
          <AuthButton label="Registrar" onPress={handleSubmit} loading={isPending} disabled={isPending} />
        </View>
      </KeyboardAvoidingView>
    </SafeAreaView>
  )
}

const styles = StyleSheet.create({
  safe: { flex: 1, backgroundColor: colors.bgBase },
  flex: { flex: 1 },
  header: {
    flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between',
    paddingHorizontal: spacing.lg, paddingVertical: spacing.md,
    borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: 'rgba(255,255,255,0.08)',
  },
  headerTitle: { ...typography.title, color: colors.textPrimary },
  headerSpacer: { width: 18 },
  scrollContent: { paddingHorizontal: spacing.lg, paddingTop: spacing.lg, paddingBottom: spacing.xl, gap: spacing.lg },
  field: { gap: spacing.xs },
  fieldLabel: { ...typography.label, color: colors.textSecondary },
  readOnly: { backgroundColor: colors.bgElevated, paddingHorizontal: spacing.md, paddingVertical: spacing.sm, borderRadius: radius.md, borderWidth: 0.5, borderColor: 'rgba(255,255,255,0.06)' },
  readOnlyText: { ...typography.body, color: colors.textTertiary },
  chips: { flexDirection: 'row', gap: spacing.xs },
  chip: { paddingHorizontal: spacing.md, paddingVertical: spacing.xs, borderRadius: radius.full, backgroundColor: colors.bgElevated, borderWidth: 0.5, borderColor: 'rgba(255,255,255,0.12)', minHeight: 44, justifyContent: 'center' },
  chipSelected: { borderColor: colors.textPrimary, borderWidth: 1 },
  chipPressed: { transform: [{ scale: 0.96 }] },
  chipLabel: { ...typography.label, color: colors.textSecondary },
  chipLabelSelected: { color: colors.textPrimary },
  footer: { paddingHorizontal: spacing.lg, paddingTop: spacing.md, paddingBottom: spacing.sm, borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: 'rgba(255,255,255,0.08)' },
})
```

- [ ] **Step 1:** Criar `app/diario/checkin.tsx`
- [ ] **Step 2:** Criar `app/diario/quick-log.tsx`
- [ ] **Step 3:** Modificar `app/_layout.tsx` (adicionar 2 Stack.Screens)
- [ ] **Step 4:** `npm run type-check` → 0 erros
- [ ] **Step 5:** Commit

```bash
git add app/diario/ app/_layout.tsx
git commit -m "feat(diario): modais checkin + quick-log + Stack.Screen registros"
```

---

## Task 9 — Tela principal (`app/(tabs)/diario.tsx`)

**Files:**
- Replace: `app/(tabs)/diario.tsx`

**Estrutura da tela:**

```typescript
// app/(tabs)/diario.tsx
import { View, Text, ScrollView, ActivityIndicator, Pressable, StyleSheet } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
import { useRouter } from 'expo-router'
import { useDiarioSummary } from '@hooks/useDiarioSummary'
import { useRegisterQuickLog } from '@hooks/useRegisterQuickLog'
import { useProfile } from '@hooks/useProfile'
import { useDoseSummary } from '@hooks/useDoseSummary'
import { QuickLogChips } from '@components/diario/QuickLogChips'
import { CheckinCard } from '@components/diario/CheckinCard'
import { DiarioTimelineItem } from '@components/diario/DiarioTimelineItem'
import { SectionHeader } from '@components/ui/SectionHeader'
import { showSuccessToast, showErrorToast } from '@lib/utils/showToast'
import { mapQueryError } from '@lib/supabase/queries/errors'
import { buildTreatmentContext } from '@lib/supabase/queries/diario'
import type { QuickLogType } from '@lib/validation/diarioSchemas'
import type { QuickLogRecord, CheckinRecord } from '@lib/supabase/queries/diario'
import { colors, typography, spacing } from '@lib/theme/tokens'

type TimelineEntry =
  | { kind: 'quick_log'; item: QuickLogRecord; sortKey: number }
  | { kind: 'checkin'; item: CheckinRecord; sortKey: number }

export default function DiarioScreen() {
  const router = useRouter()
  const { data, isLoading, isError, refetch } = useDiarioSummary()
  const { data: profile } = useProfile()
  const { data: doseSummary } = useDoseSummary()
  const { mutate: mutateQuickLog, isPending: isQuickLogPending } = useRegisterQuickLog()

  const lastDoseDate = doseSummary?.history[0]?.applicationDate ?? null
  const ctx = buildTreatmentContext(profile ?? null, lastDoseDate)

  function handleQuickLog(logType: QuickLogType) {
    mutateQuickLog(
      { logType, intensity: 2, loggedAt: new Date() },
      {
        onSuccess: () => showSuccessToast('Registrado'),
        onError: (err) => showErrorToast(mapQueryError(err)),
      }
    )
  }

  // Merge e ordena timeline por data desc
  const timeline: TimelineEntry[] = [
    ...(data?.recentQuickLogs ?? []).map((item) => ({
      kind: 'quick_log' as const,
      item,
      sortKey: item.loggedAt.getTime(),
    })),
    ...(data?.recentCheckins ?? []).map((item) => ({
      kind: 'checkin' as const,
      item,
      sortKey: new Date(item.date).getTime(),
    })),
  ].sort((a, b) => b.sortKey - a.sortKey)

  return (
    <SafeAreaView style={styles.safe} edges={['top']}>
      <ScrollView
        style={styles.flex}
        contentContainerStyle={styles.content}
        showsVerticalScrollIndicator={false}
      >
        <Text style={styles.title}>Diário</Text>

        {/* Seção 1: Registro rápido */}
        <SectionHeader title="Registro rápido" />
        <QuickLogChips onQuickLog={handleQuickLog} disabled={isQuickLogPending} />

        {/* Seção 2: Check-in do dia */}
        <SectionHeader title="Check-in do dia" />
        <CheckinCard
          todayCheckin={data?.todayCheckin ?? null}
          treatmentWeek={ctx.treatmentWeek}
          onPressCTA={() => router.push('/diario/checkin')}
        />

        {/* Seção 3: Histórico */}
        <SectionHeader title="Histórico" />

        {isLoading && (
          <ActivityIndicator
            size="small"
            color={colors.textTertiary}
            style={styles.loader}
            accessibilityLabel="Carregando histórico"
          />
        )}

        {isError && !isLoading && (
          <View style={styles.errorState}>
            <Text style={styles.errorText}>Não consegui carregar o histórico.</Text>
            <Pressable onPress={refetch} accessibilityRole="button" accessibilityLabel="Tentar novamente">
              <Text style={styles.retryText}>Tentar novamente</Text>
            </Pressable>
          </View>
        )}

        {!isLoading && !isError && timeline.length === 0 && (
          <Text style={styles.emptyText}>
            Seu diário vai aparecer aqui. Comece registrando como você está hoje.
          </Text>
        )}

        {!isLoading && !isError && timeline.map((entry) => (
          <DiarioTimelineItem
            key={entry.item.id}
            kind={entry.kind}
            item={entry.item as never}
          />
        ))}
      </ScrollView>
    </SafeAreaView>
  )
}

const styles = StyleSheet.create({
  safe: { flex: 1, backgroundColor: colors.bgBase },
  flex: { flex: 1 },
  content: { paddingHorizontal: spacing.lg, paddingBottom: spacing.xl, gap: spacing.md },
  title: { ...typography.headline, color: colors.textPrimary, paddingTop: spacing.md },
  loader: { marginVertical: spacing.md },
  errorState: { gap: spacing.xs, alignItems: 'flex-start' },
  errorText: { ...typography.body, color: colors.textSecondary },
  retryText: { ...typography.label, color: colors.textPrimary, textDecorationLine: 'underline' },
  emptyText: { ...typography.body, color: colors.textSecondary, lineHeight: 22 },
})
```

- [ ] **Step 1:** Substituir `app/(tabs)/diario.tsx`
- [ ] **Step 2:** `npm run lint` → 0 erros novos
- [ ] **Step 3:** `npm run type-check` → 0 erros
- [ ] **Step 4:** Commit principal

```bash
git add app/(tabs)/diario.tsx
git commit -m "feat(diario): tela Diário V1 com quick log + check-in + histórico"
```

---

## Task 10 — Validação via react-native-devtools-mcp (17 testes)

| # | Ação | Tool | Critério |
|---|---|---|---|
| 1 | Cold start + signin | `tap` + `type_text` (split `leonardo@teste.` + `com`) | Redirect pra Home |
| 2 | Navegar pra tab Diário | `open_deeplink('doseday:///(tabs)/diario')` | 3 seções visíveis |
| 3 | Screenshot tela inicial | `screenshot` | "Como você está hoje?" e chips visíveis |
| 4 | A11y da tela | `get_view_hierarchy` | Chips com accessibilityLabel, CTA com accessibilityRole=button |
| 5 | Tap chip "Náusea" | `tap` nas coords do chip | Haptic + pulse + toast "Registrado" |
| 6 | Verificar INSERT quick_log | `mcp__claude_ai_Supabase__execute_sql SELECT` | 1 registro com log_type='nausea', intensity=2 |
| 7 | Screenshot timeline atualizada | `screenshot` | Novo item no Histórico |
| 8 | Long-press chip "Cansaço" | `tap` com duration longo | Modal /diario/quick-log abre |
| 9 | Screenshot modal quick-log | `screenshot` | Intensity chips visíveis, campo notas visível |
| 10 | Fechar modal quick-log | `tap` em ✕ | Volta pra tela Diário |
| 11 | Tap "Fazer check-in" | `tap` | Modal checkin abre |
| 12 | Screenshot modal check-in (vazio) | `screenshot` | EmotionalStatePicker, chips de sintomas/triggers visíveis |
| 13 | Selecionar humor "Bem" + 2 sintomas + 1 trigger | `tap` × 4 | Chips selecionados |
| 14 | Tap "Salvar check-in" | `tap` | Toast "Check-in registrado" + modal fecha |
| 15 | Verificar INSERT daily_checkins | `execute_sql SELECT WHERE date=CURRENT_DATE` | symptoms com 2 itens, has_adverse_reaction=true |
| 16 | Screenshot CheckinCard atualizado | `screenshot` | Emoji 🙂 + resumo de sintomas — sem CTA |
| 17 | Limpar fixtures | `execute_sql DELETE FROM quick_logs ...; DELETE FROM daily_checkins ...` | 0 registros de teste |

---

## Task 11 — `/impeccable critique` + `/impeccable harden`

- [ ] Capturar 5 screenshots reais via `screenshot` MCP:
  1. Tela Diário inicial (antes do check-in)
  2. Chip pressed durante tap (animação)
  3. Modal check-in vazio
  4. Modal check-in preenchido (humor + sintomas + trigger selecionados)
  5. Tela Diário após check-in (CheckinCard com resumo)
- [ ] Salvar screenshots em `.impeccable/critique/screenshots/prompt15-N.png`
- [ ] Rodar `/impeccable critique` — meta ≥ 28/40. Resolver P1/P2 antes de prosseguir
- [ ] Rodar `/impeccable harden` — edge cases: tap-spam em chip, double submit, profile null, network drop durante checkin
- [ ] Resolver P1/P2 do harden

---

## Task 12 — Greps de segurança + commit final

```bash
# Garantir zero referências a generate-checkin-insight
grep -rn "generate-checkin-insight\|generateCheckinInsight" lib/ hooks/ app/
# Esperado: vazio

# Garantir zero referências a symptom_logs
grep -rn "symptom_logs" lib/supabase/queries/ hooks/
# Esperado: vazio

# Zero hardcoded hex
grep -rE "#[0-9A-Fa-f]{6}" components/diario/ app/\(tabs\)/diario.tsx app/diario/
# Esperado: vazio

# Type-check e lint finais
npm run type-check
npm run lint
```

- [ ] Todos os greps retornam vazio
- [ ] type-check → 0 erros
- [ ] lint → 0 erros novos

---

## Task 13 — PR

- [ ] Abrir PR com título: `feat(diario): tela Diário V1 com quick log + check-in + histórico (#15)`
- [ ] PR description inclui:
  - "Segundo fluxo de WRITE — Diário V1 (quick log 1-tap + check-in completo)"
  - 5 screenshots reais em markdown `![desc](url)`
  - Padrões estabelecidos: multi-select array, 1 check-in por dia, chip pulse Reanimated 4
  - Pendência: `generate-checkin-insight` ainda não chamada (Prompt 16)
- [ ] Atualizar `docs/architecture.md` seção "Aprendizados" (novos learnings pós-execução)
- [ ] Atualizar `CLAUDE.md` tabela "Histórico" com linha do Prompt 15

---

## Riscos

| # | Risco | Prob | Mitigação |
|---|---|---|---|
| R1 | `withSpring` callback Reanimated 4 — API pode ter mudado | Baixa | Verificar `~4.1.x`. Fallback: usar `withTiming` |
| R2 | `useLocalSearchParams` para `type` em quick-log — pode exigir cast | Média | Cast explícito + validação contra QUICK_LOG_TYPES |
| R3 | `Pressable.onLongPress` delay padrão 500ms — percepção de lentidão | Baixa | `delayLongPress={400}` |
| R4 | UNIQUE constraint duplicada em `daily_checkins(user_id,date)` | Info | V1 não faz migration. Registrar no aprendizado |
| R5 | `idb ui text` trunca email com `@` (Aprendizado 19) | Alta | Split: `"leonardo@teste."` + `"com"` |
| R6 | Tab bar não no accessibility tree IDB (Aprendizado 27) | Alta | `open_deeplink('doseday:///(tabs)/diario')` |
| R7 | `js_eval` não aguarda Promises (Aprendizado 13) | Alta | Usar `tap` + `type_text` + `screenshot` |
| R8 | `DiarioTimelineItem` com `item as never` — workaround de tipo | Média | Revisar com discriminated union explícita em execução |
